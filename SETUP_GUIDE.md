# نصب و راه‌اندازی داشبورد آنالیز

## 🚀 راهنمای سریع

### پیش‌نیازها

- Python 3.11 یا بالاتر
- pip (package installer for Python)

### 1. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

اگر فقط می‌خواهید سیستم را بدون GUI تست کنید:

```bash
pip install sqlalchemy cryptography pydantic pyotp PyJWT
```

### 2. راه‌اندازی دیتابیس

```bash
python init_database.py
```

این دستور:
- دیتابیس SQLite را در `~/.analysis_dashboard/dashboard.db` ایجاد می‌کند
- جداول مورد نیاز را می‌سازد
- کاربر ادمین پیش‌فرض ایجاد می‌کند (username: admin, password: admin)
- داده‌های نمونه (محصولات، فروش، آنالیتیکس) اضافه می‌کند

### 3. تست سیستم (اختیاری)

```bash
python test_system.py
```

این اسکریپت تمام قابلیت‌های اصلی را بدون نیاز به GUI تست می‌کند.

### 4. اجرای برنامه

```bash
python src/main.py
```

## 📦 ساختار دیتابیس

دیتابیس SQLite در مسیر زیر ایجاد می‌شود:
```
~/.analysis_dashboard/
├── dashboard.db       # دیتابیس SQLite
├── config.json        # تنظیمات برنامه
├── secrets.enc        # کلیدهای API رمزنگاری شده
└── .key               # کلید رمزنگاری
```

### جداول دیتابیس

1. **users** - کاربران سیستم
2. **user_sessions** - جلسات کاربران
3. **audit_logs** - لاگ عملیات
4. **dashboards** - داشبوردها
5. **widgets** - ویجت‌های داشبورد
6. **alerts** - هشدارها
7. **alert_history** - تاریخچه هشدارها
8. **analytics_data** - داده‌های آنالیتیکس
9. **sales** - فروش
10. **products** - محصولات
11. **settings** - تنظیمات
12. **report_templates** - قالب‌های گزارش
13. **cached_data** - داده‌های کش شده

## 🔑 اطلاعات ورود پیش‌فرض

- **نام کاربری:** admin
- **رمز عبور:** admin

## 🎨 قابلیت‌های پیاده‌سازی شده

### ✅ دیتابیس و Backend
- [x] SQLite برای ذخیره‌سازی داده‌ها
- [x] مدیریت کاربران با نقش‌های مختلف
- [x] سیستم احراز هویت (2FA اختیاری)
- [x] مدیریت جلسات
- [x] Audit logging
- [x] کش داده‌ها

### ✅ API Integrations
- [x] POS API Client (با mock data)
- [x] سیستم اطلاع‌رسانی (Email, Telegram, Slack)
- [ ] Google Analytics 4 (آماده برای اتصال)
- [ ] Microsoft Clarity (آماده برای اتصال)
- [ ] OpenAI/Gemini/Claude (آماده برای اتصال)

### ✅ ویجت‌ها و UI Components
- [x] MetricCard - کارت‌های متریک
- [x] StatCard - کارت‌های آماری با روند
- [x] InfoCard - کارت‌های اطلاعاتی
- [x] AlertCard - کارت‌های هشدار
- [x] LineChart - نمودار خطی
- [x] BarChart - نمودار میله‌ای
- [x] PieChart - نمودار دایره‌ای
- [x] GaugeWidget - سنجه
- [x] TableWidget - جدول
- [x] Dialogs (Settings, APIKey, Alert, Confirm, AIChat)

### ✅ داشبوردها
- [x] Performance Dashboard - بررسی عملکرد
  - کاربران فعال، جدید، بازدیدها
  - نرخ تعامل، پرش، زمان حضور
  - نمودار روند ترافیک
  - شهرهای برتر، توزیع دستگاه‌ها
  
- [x] Sales Dashboard - فروش و تجارت
  - کل فروش، تعداد سفارشات
  - میانگین سفارش، مشتریان
  - محصولات برتر
  - قیف تبدیل
  - جدول سفارشات اخیر

- [x] Settings Panel - تنظیمات
  - تنظیمات عمومی (زبان، تم، بازه به‌روزرسانی)
  - مدیریت API Keys
  - آستانه‌های هشدار
  - تنظیمات اطلاع‌رسانی

### ✅ سیستم هشدار
- [x] AlertMonitor - نظارت بر متریک‌ها
- [x] تشخیص افت ترافیک
- [x] تشخیص افزایش خطاها
- [x] تشخیص کاهش فروش
- [x] تشخیص کاهش نرخ تبدیل
- [x] ارسال نوتیفیکیشن چندکاناله

### ✅ Utilities
- [x] توابع فرمت‌دهی (عدد، ارز، درصد، تاریخ)
- [x] محاسبه تغییرات درصدی
- [x] تشخیص روند
- [x] مدیریت بازه زمانی

## 🧪 تست‌ها

### اجرای تست سیستم

```bash
python test_system.py
```

خروجی شامل:
- ✅ تست عملیات دیتابیس
- ✅ تست POS API
- ✅ تست سیستم هشدار
- ✅ تست توابع کمکی

## 📝 استفاده از داده‌های Mock

برنامه دارای سیستم تولید داده‌های Mock است که بدون نیاز به اتصال به API‌های خارجی کار می‌کند:

- **POS API**: داده‌های فروش، سفارشات، و محصولات Mock
- **Analytics**: داده‌های ترافیک و رفتار کاربران Mock
- **Notifications**: شبیه‌ساز ارسال نوتیفیکیشن

## 🔧 تنظیمات پیشرفته

### اتصال به Google Analytics 4

1. به تنظیمات بروید
2. در تب "کلیدهای API" Client ID و Client Secret را وارد کنید
3. Property IDs را وارد کنید

### اتصال به Microsoft Clarity

1. به تنظیمات بروید
2. API Key و Project IDs را وارد کنید

### اتصال به خدمات AI

1. به تنظیمات بروید
2. کلید API سرویس مورد نظر را وارد کنید:
   - OpenAI
   - Google Gemini
   - Anthropic Claude

### تنظیم اطلاع‌رسانی

1. **ایمیل**: 
   - SMTP Host و Port
   - نام کاربری و رمز عبور

2. **تلگرام**:
   - Bot Token
   - Chat ID

3. **Slack**:
   - Webhook URL

## 🐛 عیب‌یابی

### دیتابیس ایجاد نمی‌شود

```bash
# حذف دیتابیس قدیمی
rm -rf ~/.analysis_dashboard/

# ایجاد مجدد
python init_database.py
```

### خطای import module

```bash
# نصب مجدد وابستگی‌ها
pip install --upgrade -r requirements.txt
```

### خطای PyQt6

```bash
# نصب PyQt6 به صورت جداگانه
pip install PyQt6 PyQt6-WebEngine
```

## 📚 منابع بیشتر

- [مستندات SQLAlchemy](https://docs.sqlalchemy.org/)
- [مستندات PyQt6](https://www.riverbankcomputing.com/static/Docs/PyQt6/)
- [مستندات Pydantic](https://docs.pydantic.dev/)

## 🤝 مشارکت

برای گزارش باگ یا پیشنهادات، لطفاً issue ایجاد کنید.

## 📄 لایسنس

MIT License - برای جزئیات فایل LICENSE را مطالعه کنید.
